export interface Products {
  name: string | null;
  price: number | null;
  qty: number | null;
}

export interface getProducts {
  name: string | null;
  price: number | null;
  qty: number | null;
}
